# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @estore/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t estore-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system estore-auth-service && \
          adduser --system -G estore-auth-service estore-auth-service

COPY apps/auth-service/dist estore-auth-service/
COPY apps/auth-service/package.json estore-auth-service/
RUN chown -R estore-auth-service:estore-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix estore-auth-service --omit=dev -f install

CMD [ "node", "estore-auth-service" ]
